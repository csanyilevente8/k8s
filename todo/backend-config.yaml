apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: todo
data:
  DB_HOST: postgres          # the Postgres Service name — resolves within the namespace
  DB_PORT: "5432"            # note the quotes: env values must be strings
  CORS_ALLOWED_ORIGINS: https://todo.leventeprojects.xyz